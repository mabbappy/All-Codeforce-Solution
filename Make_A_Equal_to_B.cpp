#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tt;
    cin >> tt;
    while (tt--)
    {
        int n;
        cin >> n;
        int ar[n], arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> ar[n];
        }
        for (int i = 0; i < n; i++)
        {
            cin >> arr[n];
        }
    }
}