#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tt;
    cin >> tt;
    while (tt--)
    {
        int n, h, m;
        cin >> n >> h >> m;
        int x = h * 60 + m;
        int ans = 100000;
        while (n--)
        {
            int h, m;
            cin >> h >> m;
        }
    }
}